# CI overrides: longer Keycloak bootstrap, compose --wait friendly.
services:
  keycloak:
    healthcheck:
      test: ["CMD-SHELL", "bash /opt/keycloak-healthcheck.sh"]
      interval: 15s
      timeout: 10s
      retries: 30
      start_period: 180s
