FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt requirements-gates.txt ./
RUN pip install --no-cache-dir -r requirements.txt -r requirements-gates.txt cryptography pip-audit
COPY . .
ENV PYTHONPATH=/app
ENV ARTIFACTS_DIR=/app/artifacts
