FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt requirements-gates.txt ./
RUN pip install --no-cache-dir -r requirements.txt -r requirements-gates.txt cryptography httpx onnxruntime
COPY . .
ENV PYTHONPATH=/app
ENTRYPOINT ["bash", "scripts/ci/gate_model.sh"]
